class Folder {
    constructor(id, name, noteIds, createAt) {
        this.id = id;
        this.name = name;
        this.noteIds = noteIds;
        this.createAt = createAt;
    }
}

export default Folder;
